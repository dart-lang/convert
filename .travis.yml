language: dart

dart:
  - dev
  - stable

dart_task:
  - test: --platform vm
  - test: --platform chrome
  - dartanalyzer
  - dartfmt

matrix:
  exclude:
    - dart: dev
      dart_task: dartfmt

cache:
  directories:
    - $HOME/.pub-cache
